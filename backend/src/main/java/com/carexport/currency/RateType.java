package com.carexport.currency;

public enum RateType {
    /** Official rate published by the Banque d'Algerie. */
    OFFICIAL,
    /** Informal / parallel market rate. Indicative only. */
    PARALLEL
}
